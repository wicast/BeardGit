name: Build

# Manual-only. The Release workflow already builds + ships bundles on
# every `v*` tag, so we don't run on push at all — invoking this
# workflow on push to beta or on a tag would just duplicate that work.
# Keep it around as an on-demand "build the current branch and grab
# the artefacts" button for testing across macOS / Linux / Windows
# from any branch the user picks in the Actions UI.
on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: Build ${{ matrix.label }}
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
            label: macOS-arm64
            rust-targets: aarch64-apple-darwin
            target-triple: aarch64-apple-darwin
          - platform: ubuntu-22.04
            args: ''
            label: Linux-x64
            rust-targets: ''
            target-triple: x86_64-unknown-linux-gnu
          - platform: windows-latest
            args: ''
            label: Windows-x64
            rust-targets: ''
            target-triple: x86_64-pc-windows-msvc

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@1.98.0
        with:
          targets: ${{ matrix.rust-targets }}

      - uses: Swatinem/rust-cache@v2

      - name: Install system dependencies (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      - name: Install npm dependencies
        run: npm ci

      - name: Download CLI binaries
        run: node scripts/download-cli-binaries.js --target ${{ matrix.target-triple }}

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          args: ${{ matrix.args }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: BeardGit-${{ matrix.label }}
          path: |
            target/**/release/bundle/**/*.dmg
            target/**/release/bundle/**/*.app
            target/**/release/bundle/**/*.AppImage
            target/**/release/bundle/**/*.exe
          retention-days: 7
          if-no-files-found: warn
